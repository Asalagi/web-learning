function isLeap(year) {
  
    if (year % 4 === 0) {
        console.log("Leap year.")
        } else if (year % 100 !== 0) {
        console.log("Not leap year.")
        } else if (year % 400 === 0) {
        conosle.log("Leap year.")
        }
     return year;
      
    
    }
    
    isLeap(2024);